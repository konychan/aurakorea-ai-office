# 아우라코리아 AI 오피스 — 로드맵

매 작업 시작 전에 이 파일을 읽고 지금 몇 단계인지 확인한다.

1. 단계 구조 정리 + 출근 11시 + 대표실  ← 완료
2. 단계 결재 큐 — 직원이 안건을 올리고 대표가 승인/반려  ← 완료
3. 단계 캐릭터 이동 — 출근, 퇴근, 대표실 앞 줄서기 보고  ← 완료
4. 단계 실제 에이전트 1명이 진짜 업무 수행  ← 완료
5. 단계 직원 간 소통 (무한 루프 방지 제한 필수)  ← 완료
6. 단계 웹 배포 + 비밀번호 + 모바일 접속  ← 완료
7. 단계 스케줄러 (컴퓨터가 꺼져 있어도 동작)  ← 완료 (초기 로드맵 7단계 전체 완료)

---

## 조직 헌장 기반 재설계 (`ORGANIZATION.md` 참고)

| 국면 | 내용 | 상태 |
|---|---|---|
| A | 조직 재편 (본부장·팀장·팀원 계층) | **완료** |
| B | 사무실 공간 재설계 (정문·유리벽·미팅룸·휴게공간) | **완료** |
| C | 캐릭터 전면 교체 (전신 캐릭터 + 걷기 애니메이션) | **완료** |
| D | 가구·집기 (책상 두께·모니터·키보드·명패) | **완료** |
| E | 본부장 오케스트레이션 로직 + 보고 대기열 고도화 | 대기 |
| F | 회의 시스템 (소집·발언·종료) | 대기 |

### A 국면 완료 내역 (조직 재편)

- **3계층 구조 도입**: 본부장 → 팀장 → 팀원(과장·대리·사원)
- **본부장 공민규** 신설. 직원이 아니므로 `data/staff.js`가 아닌 `data/layout.js`의 `HQ_MANAGER`로 별도 관리 (대표와 동일한 방식). 대표실 아래 본부장실 배치
- **팀 구조 재편**: 기존 6개 기능팀 → **지역팀 3 + 기능팀 5 = 8팀**
  - 지역팀(`kind:'region'`): 아르헨티나 / 스페인·EU / GCC
  - 기능팀(`kind:'function'`): 브랜드소싱 / 수출서류·인증 / 물류·통관 / 재무·정산 / 마케팅·디자인
  - 기존 '해외영업본부'를 지역 데스크 단위로 분리해 지역팀으로 승격했다
- **팀장은 각 팀에 정확히 1명** (`rank:'팀장'`). 나머지는 과장·대리·사원
  - 팀장 역할: 본부장 지시를 팀원에게 전달 → 팀원 결과를 한 번 더 검증 → 본부장에게 보고. 회의 참석자
  - 팀장 자리는 금색 테두리와 명패로 시각 구분되며 팀 내 맨 앞에 정렬된다
- **인원 22명** (기존 19명 + 지역팀 보강 3명: 나윤호·진세아·하람)
  - 배시현(견적·단가)은 업무 성격상 재무·정산팀으로 이동
- **보고 경로 명문화**: 팀원 → 팀장 → 본부장 → 대표
  - 직원 상세 패널에 각자의 보고 경로가 표시된다
  - 대표 지시 시 업무 로그에 `대표 → 본부장 → 팀장 → 담당자` 전달 경로가 남고,
    완료 시 `담당자 → 팀장 검증 → 본부장 → 대표` 보고 경로가 남는다
- `data/staff.js`에 `LEADER_OF`(팀별 팀장 조회)와 `isLeader()` 제공 — E·F 국면에서 사용
- 무결성 검사 통과: 팀당 팀장 1명 / 팀·직급 유효성 / 이름 중복 없음 /
  전 팀 TASKS·AGENDAS 존재 / CHATS 인물 실존 / 본부장·대표가 STAFF에 미포함 / 유래인 `real` 플래그 보존
- 기존 1~7단계 기능(결재큐·대표실 줄서기·실시간 리서치·직원 간 대화) 전부 정상 작동 재확인

### B 국면 완료 내역 (사무실 공간 재설계)

새 공간 좌표는 전부 `data/layout.js`에 정의하고, 렌더링은 `src/office.js`가 담당한다.

- **회사 정문** (`#frontDoor`)
  - 문짝 2짝이 실제로 열리고 닫힌다. `openFrontDoor()` / `closeFrontDoor()`가 참조 카운트로 관리해
    여러 명이 동시에 드나들어도 마지막 사람이 나갈 때까지 열려 있는다
  - 출근: 문이 먼저 열리고(320ms) 캐릭터가 나타나 걸어 들어온다 — 헌장 요구대로 캐릭터가 갑자기 생성되지 않는다
  - 퇴근: 자리에서 걸어가 문 앞에 도착할 즈음 문이 열리고, 통과 후 닫힌다
  - 열리면 문짝이 안쪽으로 접히며 가운데 통로가 드러난다
  - 상단에 회사 로고(AURAKOREA)와 층 표시(1F), 하단에 `정문 · K-BEAUTY EXPORT` 안내
- **유리벽** — 정문 좌우. 사선 반사 하이라이트와 창틀 분할선, 내부가 비쳐 보이는 반투명 처리.
  왼쪽에 안내 데스크(응대 창구 포함), 오른쪽에 '아우라코리아' 벽면 로고
- **미팅룸** (`#meetingRoom`) — F 국면 회의에서 이 좌석을 그대로 쓴다
  - 원목 회의 테이블(두께·그림자), 대표 상석 / 본부장석 / 팀장 8석이 각각 명패와 함께 지정 배치
  - 프로젝터 스크린(`#projectorScreen`) + 스탠드, 화이트보드, 유리문
  - `사용 가능` / `회의 중` 상태 표시(`#meetingState`)
- **휴게 공간** (`#lounge`) — 커피머신(컵 포함) · 정수기 · 냉장고 · 간식 진열대 · 휴게 테이블 · 정리대 6종
- **업무 현황판** (`#statusBoard`) — 팀별 가동 인원 막대그래프, 처리중·대기 수, 누적 처리 건수를
  `paint()`가 돌 때마다 자동 갱신한다 (토큰을 쓰지 않는 로컬 표시)
- 팀 공간 아래에 통로를 하나 더 두어 `대표실/본부장실 → 팀 공간 → 공용 공간 → 정문` 순으로 자연스럽게 연결
- 모바일에서는 유리벽을 숨기고 정문만 1열로 표시한다
- B 국면 이후에도 기존 1~7단계 기능 전부 정상 작동 재확인

### C 국면 완료 내역 (캐릭터 전면 교체)

캐릭터 생성은 전부 `src/character.js` 한 곳에서 담당한다. 애니메이션은 100% CSS라 토큰을 쓰지 않는다.

- **전신 캐릭터로 교체** — 기존 26px 단순 아바타를 폐기하고 머리·얼굴·몸통·팔·손·다리·발을
  모두 갖춘 캐릭터로 바꿨다 (헌장 10항). 이모티콘·원형 프로필·단순 아이콘을 쓰지 않는다
- **22명 전원 고유 외형** — 헤어스타일 9종(`hs`) × 얼굴 5종(`fc`) × 피부톤 3종(`sk`) × 머리색을 조합해
  22명 전부 서로 다른 조합을 갖는다. 옷 색상만 바꾼 복제 캐릭터는 없다
- **복장으로 직책·소속 구분**
  - 팀장: 재킷 + 넥타이를 덧입어 한눈에 구분된다
  - 팀별 복장(`OUTFIT`): 셔츠 / 조끼(브랜드소싱) / 폴로(물류) / 후드(디자인)
  - 팀별 소품(`PROP`): 자료철(지역팀) · 태블릿(소싱) · 체크보드(서류) · 박스(물류) · 차트(재무) · 팔레트(디자인)
- **동작** (전부 CSS)
  - 걷기: 다리가 교차하고 팔이 반대로 흔들리며 몸이 위아래로 흔들린다. 그림자도 함께 늘었다 줄었다 한다
  - 진행 방향에 따라 좌우로 몸을 돌린다
  - 눈 깜박임 · 호흡 · 주변 살펴보기 (개체마다 시작 시점을 어긋나게 해 동시에 깜박이지 않는다)
  - 타이핑: `처리중`이면 손이 빠르게, `근무중`이면 느긋하게 움직인다
- **머리 위 상태 표시** — `근무중` / `처리중` / `보고대기` / `이동 중`을 색상 배지로 표시 (헌장 10항)
- **통로 충돌 방지** (헌장 11항) — 22명이 동시에 출근할 때 뭉치지 않도록
  개인별 출근 시차(220ms 간격)와 통로 레인 분산(5개 레인)을 적용했다
- **대표·본부장도 같은 규격** — `vipCharacter()`로 생성하고, 의자 → 캐릭터 → 책상 순으로 겹쳐
  "책상 앞에 앉아 있는" 구도를 만든다. 대표는 정장, 본부장은 정장 + 전용 넥타이 색
- 보고 대기줄에 선 캐릭터는 `char--idle`로 걷기를 멈추되 호흡·눈 깜박임은 유지한다
- C 국면 이후에도 기존 1~7단계 기능 전부 정상 작동 재확인

### D 국면 완료 내역 (가구·집기)

가구와 집기는 전부 `src/furniture.js`에서 만든다.

- **자리 구조를 3층으로 재구성** — 의자 → 캐릭터 → 책상 순으로 겹쳐
  "책상 앞에 앉아 일하는" 실제 사무실 구도를 만들었다 (기존에는 카드 위에 아바타만 떠 있었다)
- **책상은 진짜 가구** (헌장 9-5) — 평면 사각형이 아니라 살짝 내려다본 각도로
  상판 윗면(사다리꼴) · 앞면 두께 · 다리 2개 · 가로 지지대 · 발 받침 · 바닥 그림자를 갖췄다.
  팀장 책상은 더 짙은 원목을 쓴다
- **책상 위 집기 9종** (헌장 9-6) — 모니터 / 본체(타워) / 키보드 / 마우스 / 마우스패드 /
  서류 더미 / 자료철 / 메모지 / 필기구. 22개 자리 전부에 배치됨
- **팀별 전용 소품 6종** — 태블릿(브랜드소싱) · 체크보드(수출서류) · 적재 박스(물류) ·
  차트 보드(재무) · 팔레트와 붓(디자인) · 국가별 자료철(지역팀)
- **모니터에 업무 상태 표시** (헌장 9-7) — 담당자의 현재 상태에 따라 화면 색과 글자가 바뀐다:
  `작업중`(초록) / `처리중`(주황) / `보고대기`(분홍) / `자리비움`(회색) / `OFF`(퇴근)
- **자리 명패** (헌장 9-8) — 이름 · 직급 · 담당 역할. 팀장 명패는 금색 배경으로 구분
- **사무용 의자** — 등받이·좌판·기둥·다리를 갖췄고, 팀장 의자는 색이 다르다
- 자리를 비우면 사람만 사라지고 의자와 책상은 그대로 남는다
- D 국면 이후에도 기존 1~7단계 기능 전부 정상 작동 재확인
  (유래인 실시간 리서치는 확률 기반이라 한 번의 시뮬레이션에서 안 걸릴 수 있다 — 재실행으로 정상 확인)

## 현재 상태 (1단계 완료 내역)

- `aurakorea-ai-office.html` 원본은 그대로 보존, 건드리지 않음
- 코드가 `index.html / style.css / data/*.js / src/*.js / status.json` 구조로 분리됨
- 근무시간은 `data/layout.js`의 `WORK_HOURS` 한 곳만 고치면 전체가 바뀜 (현재 출근 11:00, 근무 11:00~20:00)
- 사무실 상단 중앙에 대표실(장경철·대표) 추가. 대표는 `data/staff.js`(직원 명단)에 포함되지 않고 `data/layout.js`의 `CEO` 로 별도 관리됨
- 대표실 문 앞 보고 대기줄 좌표 5칸이 `data/layout.js`의 `CEO_QUEUE`에 정의됨 (3단계에서 사용 예정)
- `data/layout.js`에 방/책상/복도/정문 좌표 체계가 정의되어 있고, 각 방·책상 DOM에 `data-col/data-row` 속성으로 심어져 있어 3단계 캐릭터 이동에서 그대로 읽어 쓸 수 있음
- 상단에 근무중/처리중/보고대기/미출근 상태 요약 칩 추가됨

## 현재 상태 (2단계 완료 내역)

- 근무 중인 직원이 무작위로 결재 안건(`data/staff.js`의 `AGENDAS`)을 올리면 대표 콘솔 옆 "결재 큐" 패널에 뜬다
- 안건을 올린 직원은 "보고대기" 상태가 되어 자리에서 대기하고, 대표가 승인/반려를 누르면 업무 로그에 기록되고 다시 근무중으로 돌아간다
- 큐 상태와 승인/반려 로직은 `src/office.js`(`agendaQueue`, `submitAgenda`, `resolveAgenda`)에 있고, 안건 발생 확률과 버튼 연결은 `src/sim.js`에 있다

## 현재 상태 (3단계 완료 내역)

- 직원이 실제로 화면 위를 걸어서 이동한다: 출근(정문→책상), 퇴근(책상→정문), 보고(책상→대표실 앞 대기줄 한 칸→책상)
- 이동 중에는 책상 아바타가 숨겨지고, 대표실 앞 대기줄 슬롯에 도착하면 그 자리에 본인 아바타가 표시된다(승인/반려되면 다시 책상으로 걸어서 복귀)
- 이동 로직은 `src/office.js`의 `arriveWalk` / `leaveWalk` / `goReport` / `returnFromReport`에 있고, 화면상 실제 좌표(getBoundingClientRect)를 기준으로 이동하며 같은 직원의 이동 동작은 겹치지 않도록 순서대로 처리된다(`walkLocks`)
- 결재 큐(2단계)와 대표 직접 지시(기존 콘솔) 양쪽 모두 "대표실 앞 줄서기"를 공유해서 사용한다
- 알려진 제한: 근무 종료 시각에 결재 대기 중(보고대기)인 직원은 그날 퇴근 처리가 되지 않고 대기 상태로 남는다 — 추후 단계에서 필요시 보완

## 현재 상태 (4단계 완료 내역)

- 1호 실제 에이전트: 유래인 (브랜드소싱팀 · 브랜드 발굴). `data/staff.js`에서 `real:true`로 표시됨
- 정적 파일 서버 대신 `server.js`(순수 Node, 의존성 없음)로 교체. 정적 파일 서빙 + `/api/agent/:이름` 라우트를 함께 제공
- 유래인은 근무 중 사내 시각 기준 약 1시간에 한 번꼴로 자동으로 실제 클로드 API(웹 검색 도구 포함)를 호출해 진짜 K뷰티 브랜드 후보를 조사한다 (`src/agent.js`의 `runRealAgent`, `sim.js`의 쿨다운 로직)
- 결과는 지어낸 내용이 아니라 실제 검색 결과에 근거해서만 작성되도록 시스템 프롬프트로 제한했고, 자리를 클릭하면 상세 패널에서 전체 내용을 볼 수 있다
- API 키는 프로젝트 루트의 `.env`(git 제외 대상)에 `ANTHROPIC_API_KEY=...` 형식으로 저장하고 서버에서만 사용한다. 키가 없으면 "리서치 실패"로 정직하게 표시되고 나머지 기능은 그대로 동작한다
- 기존 대표 콘솔(대표가 직접 지시하는 기능)은 이번 단계에서 손대지 않았다 — 여전히 별도 상태로, 앤스로픽 키 연동은 다음에 필요하면 별도로 진행

### 결정 변경: 실시간 유료 검색 → 무료 정적 결과 파일

- 대표님 계정에 Anthropic API 크레딧이 없어서, 서버가 실시간으로 유료 API를 호출하는 방식 대신 **Claude Code(대표님이 이미 쓰는 플랜)가 직접 웹 검색을 수행해 `data/agent-results.json`에 결과를 저장**하고, 유래인은 근무 중 그 파일을 확인해서 보고하는 방식으로 바꿨다
- `server.js`의 `/api/agent/:name` 유료 실시간 검색 경로는 코드에 남겨뒀다 — 나중에 크레딧을 충전하면 그쪽으로 전환 가능
- 최신 정보로 갱신하려면 대표님이 "다시 검색해줘"라고 요청하면 된다 (완전 자동 스케줄링은 7단계에서 다룬다)
- 부수적으로 발견한 버그 수정: 직원 자리를 클릭했을 때 상단 "사내 근무시각" 시계가 NaN:NaN으로 깨지던 문제(`select()` 호출 시 simMin 누락)를 고쳤다

## 현재 상태 (5단계 완료 내역)

- 근무 중인 직원이 정해진 상대(예: 유래인→천도윤, 한지우→정하율, 배시현→남우리 등, `data/staff.js`의 `CHATS`)에게 실제로 걸어가서 묻고, 상대가 미리 정한 답변으로 답하는 모습을 볼 수 있다
- 무한 루프 방지 3중 장치: (1) 질문→답변 1회로 끝나는 단방향 구조라 답변이 새 질문을 만들지 않음 (2) 사무실 전체에서 동시에 대화는 1건만 허용(`chatActive`) (3) 발신자별 쿨다운(45분)으로 연달아 말 걸지 못하게 제한 — 전부 `src/office.js`의 `chatWith`/`_chatWith`에 있다
- 대화 중인 상대(근무중·처리중이 아닌 직원)에게는 말을 걸지 않도록 상태를 확인한다
- 이 대화도 4단계와 마찬가지로 실제 유료 API를 쓰지 않고 미리 써둔 대사를 사용한다 — 루프 위험이 있는 기능에 과금 API를 얹는 건 비용 폭주 위험이 있다고 판단했다

## 현재 상태 (6단계 완료 내역)

- Vercel(무료 플랜)에 배포됨. 주소: https://aurakorea-office.vercel.app
- 접속 시 비밀번호를 입력해야 들어갈 수 있다(로그인 게이트). 비밀번호는 Vercel 환경변수 `SITE_PASSWORD`에만 저장되어 있고 코드/깃에는 없다. 맞는 비밀번호를 입력하면 서명된 쿠키가 발급되어 이후 접속(모바일 포함)은 자동으로 통과된다
- 로그인 관련 코드: `api/login.js`(비밀번호 확인, 쿠키 발급), `api/session-check.js`(쿠키 검증), `index.html`의 `authGate`/`boot()`
- 모바일 브라우저에서 접속하면 화면이 세로 1열로 자동 정리된다(기존 반응형 CSS 그대로 작동)
- 로컬 개발 서버 파일명을 `server.js` → `dev-server.js`로 바꿨다. Vercel이 "server.js"라는 이름을 보고 이 프로젝트를 "커스텀 Node 서버"로 오인해서 정적 파일과 로그인 API가 전부 막히는 문제가 있었다(원인 파악 후 수정). 로컬에서 실행할 때는 이제 `node dev-server.js`로 띄운다
- 로컬(`node dev-server.js`)에서는 로그인 API가 없으므로 비밀번호 없이 바로 들어가진다 — 개발할 때 편하도록 의도한 동작
- `vercel.json`에 `"framework": null`을 명시해 자동 프레임워크 감지를 끄고 정적 사이트로 확실히 배포되게 했다

## 현재 상태 (7단계 완료 내역)

- GitHub 저장소: https://github.com/konychan/aurakorea-ai-office (공개)
  - Claude 클라우드 에이전트가 접근하려면 저장소 권한 승인이 필요한데, 비공개 상태에서 계속 막혀서 대표님 승인 하에 공개로 전환했다. 민감 정보(.env, 비밀번호)는 저장소에 없다.
- Vercel ↔ GitHub 연결 완료: master에 푸시하면 약 30초 내 자동 배포된다 (사람이 배포를 누를 필요 없음)
- Claude 클라우드 루틴 "유래인 리서치 자동 갱신" 생성:
  - 루틴 ID: `trig_01MW58DfmwXpLSyR3x9XgxYs`
  - 관리 화면: https://claude.ai/code/routines
  - 일정: 매일 한국시간 오전 9시 (UTC 기준 `0 0 * * *`)
  - 동작: 앤스로픽 클라우드에서 실제 웹 검색 → `data/agent-results.json` 갱신 → 커밋·푸시 → Vercel 자동 배포
  - 컴퓨터가 꺼져 있어도 동작한다 (클라우드 실행)
- 실행 검증 완료: 1차 실행은 GitHub 쓰기 권한 부족으로 403 실패 → Claude GitHub 앱(https://github.com/apps/claude) 설치 후 2차 실행에서 커밋·푸시·배포까지 성공 확인
- 이 방식은 대표님의 기존 Claude 플랜을 사용하므로 Anthropic API 크레딧 충전이 필요 없다
